// The store will be able to hold JSON objectwhich conform to a JSON schema
// It must be possible to perform fast search on the store
// It must also be possible to search concurrently
//
// Each object might represent a command or a format
//
// The object will have a key which identifies it on the speech string
// which is sent in from the parser
